# dotnet
**/bin/
**/obj/
*.user

# node / frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/playwright-report/
frontend/test-results/

# env + ide
.env
.env.local
# local app settings with real credentials (the .example stays committed)
appsettings.Development.json
.vs/
.idea/
.vscode/

# os
.DS_Store

# local archives / assignment docs (not part of the source)
Archive.zip
*.pdf

# pnpm lockfile (repo uses npm) — artifact of a local workaround
pnpm-lock.yaml

# typescript incremental build cache
frontend/*.tsbuildinfo

# pnpm workspace config (repo uses npm)
frontend/pnpm-workspace.yaml
